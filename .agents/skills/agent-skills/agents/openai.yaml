interface:
  display_name: "Agent Skills"
  short_description: "Repo-specific patterns and workflows for agent-skills"
  default_prompt: "Use the agent-skills repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true